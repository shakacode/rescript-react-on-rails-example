{
  "name": "app",
  "private": true,
  "scripts": {
    "clean": "rescript clean",
    "format": "rescript format -all",
    "prebuild": "yarn clean && yarn format",
    "start": "yarn prebuild && rescript build -w -with-deps",
    "build": "yarn prebuild && rescript build -with-deps"
  },
  "dependencies": {
    "@rails/webpacker": "^3.4.1",
    "@rescript/react": "0.10.3",
    "babel-preset-react": "^6.24.1",
    "bs-react-on-rails": "^0.0.1",
    "prop-types": "^15.6.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-on-rails": "^11.1.7",
    "rescript": "9.1.4"
  },
  "devDependencies": {
    "webpack-dev-server": "2.11.2"
  }
}
